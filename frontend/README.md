# Frontend Store

Aquí va la **compilación** del frontend (build estático).

Cuando tengas el build (Vite/etc.), copiá el contenido de `dist/` a esta carpeta
(`index.html`, `assets/`, …) o apuntá el servidor web a ese output con
`BASE_PATH=/store/`.
